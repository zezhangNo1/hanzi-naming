/**
 * aigc-banner — AI 生成标识横幅（T14 完整实现前的占位接入）
 *
 * 合规要求（交接包第七节 C-2）：生成页顶部固定展示「内容由 AI 生成，仅供文化参考」，
 * 不可关闭、非角落小字。B2 静态占位；T14 将叠加编号/模型声明等完整要素。
 */
'use strict';

Component({
  properties: {},
  data: {
    text: '内容由 AI 生成，仅供文化参考'
  }
});
